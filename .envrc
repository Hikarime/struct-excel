use flake

VENV=.venv

if [ ! -d "$VENV" ]; then
    python -m venv $VENV
fi

source $VENV/bin/activate

if [ -f requirements.txt ]; then
    pip install -r requirements.txt
fi

if [ -f pyproject.toml ] && ! pip show struct-excel &>/dev/null; then
    pip install -e .
fi
